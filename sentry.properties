defaults.url=https://sentry.io/
defaults.org=hopeai-rh
defaults.project=javascript-nextjs

# Auth token for Sentry CLI (optional, can also be set via SENTRY_AUTH_TOKEN env var)
# auth.token=your_auth_token_here

# CLI configuration
cli.executable=node_modules/.bin/sentry-cli